# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'mvvmsampleapp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for mvvmsampleapp
  pod 'SwiftLint'
  pod 'SDWebImage', '~> 4.0'
  target 'mvvmsampleappTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
